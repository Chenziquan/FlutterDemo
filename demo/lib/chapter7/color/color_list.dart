import 'package:flutter/material.dart';

class ColorListRoute extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    List colors = [50, 100, 200, 300, 400, 500, 600, 700, 800, 900];
    return Column(
      children: [
        
      ],
    );
  }
}
